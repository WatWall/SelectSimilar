#pragma once

// Dialog and control IDs for the live-preview adjuster.
// Resource numbers offset from core/resource.h (which uses 100-300).
#define IDD_ADJUSTER_DIALOG          400

// Compare combo (replaces the 3-radio group)
#define IDC_COMPARE_LABEL            401
#define IDC_COMPARE_COMBO            402

// Threshold (CustEdit + SpinnerControl pair, + unit suffix label)
#define IDC_THRESH_LABEL             410
#define IDC_THRESH_EDIT              411
#define IDC_THRESH_SPIN              412
#define IDC_THRESH_UNIT              413

#define IDC_COUNT_LABEL              420

// IDOK (Apply) and IDCANCEL (Cancel) come from <windows.h>.
